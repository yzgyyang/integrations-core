version: "3.9"

services:

# Caddy will create host a site with the metrics to exposes
  vsphere:
    image: vmware/vcsim:latest
    ports:
      - "8989:8989"
    command:
      - "-api-version"
      - "7.0"
      - "-l" 
      - "0.0.0.0:8989"